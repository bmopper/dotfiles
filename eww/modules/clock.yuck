(defwidget clock []
  (box :orientation "v" :space-evenly false :halign "center"
    (label :class "clock" :text {formattime(EWW_TIME, "%H:%M")})
    (label :class "date" :text {formattime(EWW_TIME, "%d/%m/%y")}) 
  )
)
